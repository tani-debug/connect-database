This file has login signup page which is connected to mongo db database. all the login and signup details enters, stored at the database.

to run it locally:
install all npm packages used, hbs, express and mongoose.
with the help of:  npm i hbs express mongoose nodemon
and then run it locally using: nodemon src/server.js
